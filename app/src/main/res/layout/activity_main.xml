<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollView_activityMain_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/linearLayout_activityMain_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <!-- Get Request. -->
        <Button
            android:id="@+id/button_activityMain_testGetRequest"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/test_get_request" />

        <!-- Post Request. -->
        <Button
            android:id="@+id/button_activityMain_testPostRequest"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/test_post_request" />

        <!-- Put Request. -->
        <Button
            android:id="@+id/button_activityMain_testPutRequest"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/test_put_request" />

        <!-- Delete Request. -->
        <Button
            android:id="@+id/button_activityMain_testDeleteRequest"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/test_delete_request" />

    </LinearLayout>
</ScrollView>
